[package]
name = "certcheck"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Check TLS certificate details for a host"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
